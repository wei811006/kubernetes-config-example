apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetes-config-example.fullname" . }}-test-connection"
  labels:
{{ include "kubernetes-config-example.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubernetes-config-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
